function calcular(tipo, valor) {
  if (tipo === 'acao') {
    
  } else if (tipo === 'valor') {
    var resultado = document.getElementById('tela_resultado').value;
    resultado = "valor"
  }

  conso
}